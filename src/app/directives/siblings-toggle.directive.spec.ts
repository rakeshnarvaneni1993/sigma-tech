import { SiblingsToggleDirective } from './siblings-toggle.directive';

describe('SiblingsToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new SiblingsToggleDirective();
    expect(directive).toBeTruthy();
  });
});
