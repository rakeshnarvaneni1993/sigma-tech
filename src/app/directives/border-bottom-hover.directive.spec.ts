import { BorderBottomHoverDirective } from './border-bottom-hover.directive';

describe('BorderBottomHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new BorderBottomHoverDirective();
    expect(directive).toBeTruthy();
  });
});
